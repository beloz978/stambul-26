# ── stambul-26 · non-secret config ──────────────────────────────────────────
# Rendered to config/.env.config by config/.env-render.sh (flat, no expansion).
# Secrets live in config/.env.secrets (git-ignored) — NEVER here.

WORKER_NAME=${WORKER_NAME:-stambul-26}
WORKER_URL=${WORKER_URL:-https://stambul-26.pkvxmch86y.workers.dev}
CF_API_BASE=${CF_API_BASE:-https://api.cloudflare.com/client/v4}

# Telegram informing (bot token comes from ~/.ai/.env.secrets → TG_BOT_TOKEN)
# Target: «My pets projects» forum, тред https://t.me/c/2281796095/1519
TG_CHAT_ID=${TG_CHAT_ID:--1002281796095}
TG_THREAD_ID=${TG_THREAD_ID:-1519}
